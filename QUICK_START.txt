━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  YOOSKY STORE — PANDUAN SETUP LENGKAP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 1 — SUPABASE SCHEMA (wajib, sekali saja)
─────────────────────────────────────────────
1. Buka: https://supabase.com/dashboard → project kamu
2. Klik "SQL Editor" → "New Query"
3. Copy-paste isi file: supabase-schema.sql
4. Klik "Run"

STEP 2 — ENV VARIABLES DI VERCEL
─────────────────────────────────
Set ini di Vercel Dashboard → Settings → Environment Variables:

  ┌─────────────────────────────────────────────────────────────────┐
  │  WAJIB (tanpa ini website tidak jalan)                          │
  ├──────────────────────────────┬──────────────────────────────────┤
  │  SUPABASE_URL                │ https://xxxx.supabase.co         │
  │  SUPABASE_SERVICE_ROLE_KEY   │ eyJxx... (service role, bukan    │
  │                              │ anon key!)                       │
  │  SESSION_SECRET              │ string random panjang, bebas     │
  │  INITIAL_ADMIN_USERNAME      │ Abdurahman Mulvi                 │
  │  INITIAL_ADMIN_PASSWORD      │ Tarakan11#                       │
  ├──────────────────────────────┴──────────────────────────────────┤
  │  OPSIONAL (fitur tambahan)                                      │
  ├──────────────────────────────┬──────────────────────────────────┤
  │  SUPABASE_ANON_KEY           │ eyJxx... (anon key, opsional)    │
  │  TURNSTILE_SITE_KEY          │ dari Cloudflare Turnstile        │
  │  TURNSTILE_SECRET_KEY        │ dari Cloudflare Turnstile        │
  │  PORT                        │ default 3000 (tidak perlu diset  │
  │                              │ di Vercel)                       │
  └──────────────────────────────┴──────────────────────────────────┘

  Cara dapat SUPABASE_SERVICE_ROLE_KEY:
  → Supabase Dashboard → Project → Settings → API → service_role key

  Cara dapat TURNSTILE keys (opsional, untuk proteksi login):
  → https://dash.cloudflare.com → Turnstile → Add site
  → Site Key  = TURNSTILE_SITE_KEY
  → Secret Key = TURNSTILE_SECRET_KEY

STEP 3 — PUSH SETTINGS + LOGO KE SUPABASE (WAJIB!)
────────────────────────────────────────────────────
Jalankan SEKALI dari komputer lokal (bukan Vercel):

  1. Buat file .env berisi env variables di atas
  2. Jalankan: npm install
  3. Jalankan: node seed-settings.js

Output yang benar:
  ✅ Logo diupload ke Supabase Storage: https://...
  ✅ BERHASIL disimpan ke Supabase!
  │ adminUsername: rei
  │ hash verify  : ✅ OK

STEP 4 — DEPLOY KE VERCEL
──────────────────────────
  vercel --prod
  (atau push ke GitHub, Vercel auto-deploy)

STEP 5 — LOGIN ADMIN
─────────────────────
  URL      : https://<nama-project>.vercel.app/admin-login
  Username : Abdurahman Mulvi
  Password : Tarakan11#

STEP 6 — UPLOAD BANNER (dari Admin Panel)
──────────────────────────────────────────
  Admin → Banner → Upload gambar banner YOOSKY STORE → Save

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  GANTI CREDENTIALS (kapanpun)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Edit seed-settings.js bagian:
  ADMIN_USERNAME = 'Abdurahman Mulvi'
  ADMIN_PASSWORD = 'Tarakan11#'
Lalu jalankan: node seed-settings.js

ATAU dari Admin Panel → Settings → Kredensial Admin

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  TROUBLESHOOTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
❌ Admin login tidak bisa masuk
   → Jalankan: node seed-settings.js
   → Pastikan output "hash verify : ✅ OK"

❌ Logo tidak muncul
   → Jalankan: node seed-settings.js (upload logo ke Supabase Storage)
   → Atau set logoUrl manual di Admin → Settings → URL Logo

❌ Banner tidak muncul
   → Masuk Admin → Banner → Upload banner baru
   → Banner yang diupload otomatis tersimpan di Supabase Storage

❌ Error "relation keyvalue_store does not exist"
   → Jalankan ulang supabase-schema.sql di SQL Editor Supabase

❌ Upload gambar (produk/banner/avatar) gagal setelah update 23 Agu 2026
   → Update ini menambahkan dependency baru "sharp" (untuk kompres
     otomatis gambar yang diupload -- lihat PERUBAHAN 23 AGU 2026 di
     bawah). Kalau deploy manual (bukan lewat Vercel auto-deploy dari
     Git), jalankan "npm install" ulang dulu supaya sharp ter-install.
   → Kalau ini terjadi di lingkungan lokal (bukan Vercel) dan sharp
     gagal install, cek versi Node.js (sharp butuh Node 18+).

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  PERUBAHAN 23 AGU 2026 (fix performa loading lambat)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PageSpeed Insights sebelumnya menunjukkan LCP 66 detik di mobile dan
total payload halaman ~14MB, hampir semuanya gambar. Penyebabnya:
tidak ada proses kompres/resize sama sekali saat upload -- file yang
diupload admin (produk/banner/avatar/QRIS) disimpan & disajikan apa
adanya, sebesar apapun aslinya.

Sekarang SETIAP upload gambar baru otomatis:
  - Di-resize maksimal 1600px sisi terpanjang
  - Dikonversi ke WebP kualitas 80 (jauh lebih kecil dari PNG/JPG
    mentah, kualitas visual hampir tidak beda)
  - GIF dikecualikan (dibiarkan apa adanya supaya animasi tidak hilang)

PENTING: ini cuma berlaku untuk gambar yang diupload SETELAH update
ini di-deploy. Untuk gambar produk/banner LAMA yang sudah kadung besar,
migrasikan lewat salah satu cara berikut (tidak perlu re-upload manual
satu-satu):

CARA PALING GAMPANG (lewat browser, tidak perlu .env / terminal / login
admin sama sekali -- pakai env Supabase yang sudah ada di Vercel):
  1. Set SETUP_SECRET di Vercel Environment Variables (string acak
     rahasia apa saja, cuma kamu yang tahu) -- redeploy setelah diisi.
  2. Buka: https://fauzistore.web.id/admin/migrate-images?secret=ISI_SECRET_KAMU
     (ganti ISI_SECRET_KAMU dengan value dari langkah 1)
  3. Progress muncul live di halaman. Kalau ada tulisan "Sisa belum
     diproses" di akhir, refresh/buka lagi URL yang sama untuk lanjutkan
     (satu panggilan cuma proses maks 15 file dulu supaya tidak kena
     timeout Vercel -- aman diulang, file yang sudah selesai otomatis
     dilewati).
  4. Setelah selesai, HAPUS SETUP_SECRET dari Vercel Environment
     Variables -- endpoint ini otomatis nonaktif total (403) kalau
     SETUP_SECRET tidak di-set, jadi aman dari akses tak sah selama
     env itu kosong.

CARA ALTERNATIF (dari terminal lokal, kalau .env sudah terisi
SUPABASE_URL & SUPABASE_SERVICE_ROLE_KEY):
    node migrate-images-to-webp.js

Kedua cara memakai logic yang sama: mengambil semua gambar lama di
Supabase Storage, mengompresnya jadi WebP, memperbarui semua referensi
URL di products.json/users.json/settings.json, lalu menghapus file
lama. Aman dijalankan/diakses lebih dari sekali (file yang sudah .webp
otomatis dilewati).
